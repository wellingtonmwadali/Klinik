# Base URL of the klinik-api Django backend, called server-to-server from
# Server Components / Server Actions. The browser never talks to this
# directly. Copy this file to .env.local and adjust if needed.
DJANGO_API_URL=http://localhost:8000
